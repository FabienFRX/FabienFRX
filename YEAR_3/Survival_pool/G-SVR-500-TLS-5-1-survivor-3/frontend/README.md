# Frontend React (Vite + TypeScript)

## Quick Start

Install dependencies and start the development server:

```
cd frontend
npm install
npm run dev
```

Open your browser at the shown URL (default: http://localhost:5173).

## Structure
- `public/` static files
- `src/main.tsx` entrypoint
- `src/App.tsx` root component
- `src/components/` reusable components
- `src/hooks/` custom hooks
- `src/pages/` pages (future routing)

## TODO Next Steps
- Add an API client (fetch/axios)
- Set up routing (react-router-dom)
- Integrate Tailwind or another UI library
- State management (zustand / redux-toolkit) if needed
