/*
** EPITECH PROJECT, 2024
** C++
** File description:
** main.cpp
*/
#include "head.hpp"

using namespace std; //Indique quel espace de noms on va utiliser

int main(int argc, const char *argv[])
{
    first_code();
    first_loop(argc, argv);
    first_math();
    second_math();
    reference_code();
    first_table_dyn();
    first_condition();
    first_condition();
    write_in_file();
    read_file();
    size_file();
    first_pointer();
    return 0;
}